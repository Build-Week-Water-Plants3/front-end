import React, {useState, useEffect} from 'react';
import * as yup from 'yup';

const Form = (props) => {
    return (
        <form action="">
            <input type="text">Name of Plant:</input>
        </form>
    )
}

export default Form;

